{
  "name": "optimacros-test",
  "version": "1.0.0",
  "description": "by Aleksandr Piliugin",
  "main": "",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client && npm run install && npm run build --prefix server",
    "start": "npm start --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aleksandr Piliugin",
  "license": "ISC"
}
